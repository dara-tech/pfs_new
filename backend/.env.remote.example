# Copy these DB_* lines into backend/.env to use psfnew on the Windows server from your Mac.
# Requires tunnel: npm run db:tunnel (separate terminal)

DB_HOST=127.0.0.1
DB_PORT=3307
DB_DATABASE=psfnew
DB_USERNAME=root
DB_PASSWORD=your-windows-mysql-root-password
