<IfModule mod_rewrite.c>
  RewriteEngine On
  # /api is proxied by VirtualHost — do not serve SPA for API paths
  RewriteRule ^api(/|$) - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
